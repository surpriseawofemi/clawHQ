version: '3'

vars:
  APP_NAME: "ClawHQ"
  BIN_DIR: "bin"
  PACKAGE_MANAGER: '{{.PACKAGE_MANAGER | default "npm"}}'
  VITE_PORT: '{{.WAILS_VITE_PORT | default 9245}}'
  # Target OS for build/package/run. Defaults to the host OS, and is overridden
  # by `wails3 build GOOS=...` (or the GOOS env var) for cross-compilation. The
  # tasks below dispatch to the matching platform Taskfile via this variable.
  GOOS: '{{.GOOS | default OS}}'

includes:
  common: ./build/Taskfile.yml
  windows: ./build/windows/Taskfile.yml
  darwin: ./build/darwin/Taskfile.yml
  linux: ./build/linux/Taskfile.yml
  ios: ./build/ios/Taskfile.yml
  android: ./build/android/Taskfile.yml

tasks:
  build:
    summary: Builds the application
    cmds:
      - task: "{{.GOOS}}:build"

  package:
    summary: Packages a production build of the application
    cmds:
      - task: "{{.GOOS}}:package"

  run:
    summary: Runs the application
    cmds:
      - task: "{{.GOOS}}:run"

  release:
    summary: Bumps the patch version, packages, commits, tags and pushes so CI ships the release
    desc: |
      One command per release. Bumps VERSION (or use VERSION=x.y.z), packages the app
      for this machine, commits everything, tags vX.Y.Z and pushes main plus the tag.
      CI builds the Windows and macOS releases from the tag; installs on other
      machines update themselves from it. The bundle built here is the same version,
      so this machine does not offer itself the update.
    vars:
      CURRENT:
        sh: tr -d '[:space:]' < VERSION
      NEXT: '{{.VERSION | default (printf "%s.%s.%d" (index (splitList "." .CURRENT) 0) (index (splitList "." .CURRENT) 1) (add (atoi (index (splitList "." .CURRENT) 2)) 1))}}'
    preconditions:
      - sh: git diff --quiet VERSION
        msg: "VERSION has uncommitted changes; commit or revert them first."
      - sh: '! git rev-parse -q --verify "refs/tags/v{{.NEXT}}" >/dev/null'
        msg: "tag v{{.NEXT}} already exists; pass VERSION=x.y.z"
    cmds:
      - printf '%s\n' "{{.NEXT}}" > VERSION
      - task: package
      - git add -A
      - git commit -m "Release v{{.NEXT}}"
      - git tag "v{{.NEXT}}"
      - git push origin HEAD "v{{.NEXT}}"
      - 'echo "Released v{{.NEXT}}. CI is building the Windows and macOS packages."'

  dev:
    summary: Runs the application in development mode
    cmds:
      - wails3 dev -config ./build/config.yml -port {{.VITE_PORT}}

  install:msix:tools:
    summary: Installs tools required for Windows MSIX packaging
    cmds:
      - task: windows:install:msix:tools

  setup:docker:
    summary: Builds Docker image for cross-compilation (~800MB download)
    cmds:
      - task: common:setup:docker

  build:server:
    summary: Builds the application in server mode (no GUI, HTTP server only)
    cmds:
      - task: common:build:server

  run:server:
    summary: Runs the application in server mode
    cmds:
      - task: common:run:server

  build:docker:
    summary: Builds a Docker image for server mode deployment
    cmds:
      - task: common:build:docker

  run:docker:
    summary: Builds and runs the Docker image
    cmds:
      - task: common:run:docker
